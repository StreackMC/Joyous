# 2026-07-23 工作日志

## SMenu 重命名为 JMenu
- 包路径: `com.github.streackmc.Joyous.SMenu` → `JMenu`
- 类名: SMenuMain/Manager/Entry/Listener/Command → JMenu*
- 命令: `/smenu` → `/jmenu`
- 权限: `joyous.commands.smenu.*` → `joyous.commands.jmenu.*`
- 配置节: `SMenu:` → `JMenu:`
- i18n 键: `smenu.*` → `jmenu.*`
- PDC Key: `smenu_path` → `jmenu_path`
- 菜单目录: `models/SMenu/` → `models/JMenu/`
- 模板文件: `SMenu.default.json` → `JMenu.default.json`
- 运行时配置文件(mcserver/)同步更新
- Git commit: `600e2d4` (Neonai)

## SpigotAPI 弃用 → PaperAPI 迁移
- 通过 VSCode-MCP 获取 IDE 诊断，识别所有弃用警告
- **Player.getDisplayName()** → `player.displayName()` 返回 Component，需 `LegacyComponentSerializer` 序列化
  - EntroprixCommand.java: 4 处 (lines 140, 144, 165, 187)
  - WebStatusAPI.java: 3 处 (lines 93, 95, 96)
- **Server.broadcastMessage(String)** → `Server.broadcast(Component)` 解析 legacy color codes
  - RestarterMain.java: 6 处
  - RestarterCommand.java: 1 处
- **Player.kickPlayer(String)** → `Player.kick(Component)`
  - RestarterMain.java: 1 处
- **Server.getMotd()** → `Server.motd()` 返回 Component
  - WebStatusAPI.java: 1 处
- **Plugin.getDescription().getVersion()** → `Plugin.getPluginMeta().getVersion()`
  - entry.java: 1 处
- **JavaPlugin.getPluginLoader().disablePlugin()** → `PluginManager.disablePlugin()`
  - i18n.java: 1 处
- 编译: mvn clean compile BUILD SUCCESS，弃用警告清零
- Git commit: `2983957` (Neonai)
